{
  "name": "@solaxis/cli",
  "version": "0.1.0",
  "private": true,
  "description": "Solaxis Standalone Terminal CLI Runner",
  "bin": {
    "solaxis": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo 'CLI tests will be configured in Unit 05'",
    "lint": "echo 'Linting @solaxis/cli...'"
  },
  "dependencies": {
    "@solaxis/shared": "workspace:*",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
