[package]
name = "solaxis-engine-sdk"
version = "0.1.0"
description = "Reusable on-chain Rust SDK for authoring Solaxis micro-instance compute kernels"
edition = "2021"

[dependencies]
anchor-lang = "0.32.1"
ephemeral-rollups-sdk = { version = "0.16.2", features = ["anchor-compat"] }
