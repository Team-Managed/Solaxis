{
  "name": "@solaxis/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Solaxis Anchor Engine Smart Contract Workspace",
  "scripts": {
    "build": "anchor build",
    "typecheck": "echo 'No TypeScript in contracts'",
    "test": "cargo test --manifest-path programs/solaxis_engine/Cargo.toml",
    "lint": "cargo clippy --manifest-path programs/solaxis_engine/Cargo.toml -- -D warnings"
  }
}
