{
  "name": "@solaxis/app",
  "version": "0.1.0",
  "private": true,
  "description": "Solaxis Web3 Developer Console",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "echo 'App tests will be configured in subsequent units'",
    "lint": "echo 'Linting @solaxis/app...'"
  },
  "dependencies": {
    "@solaxis/shared": "workspace:*",
    "@solana/web3.js": "^1.98.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.7.3"
  }
}
