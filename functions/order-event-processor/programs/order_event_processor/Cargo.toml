[package]
name = "order_event_processor"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
anchor-lang = "0.32.1"
ephemeral-rollups-sdk = { version = "0.16.2", features = ["anchor-compat"] }

[features]
idl-build = ["anchor-lang/idl-build"]
